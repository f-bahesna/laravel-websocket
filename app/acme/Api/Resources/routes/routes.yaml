ping:
    type: presenter
    methods: [get]
    path: ping
    middleware: ["auth:user"]
    presenter: ping

auth:
    type: group
    children: _auth.yaml

user_chatroom:
    type: group
    children: _user_chatroom.yaml