chatroom:
    type: resource
    resource: chatroom
    path: chatrooms
    methods: [GET]
    only: [ index ]
    middleware: ["auth:user"]
    options:
        paginate: true
        withs:
            index:
                - messages
        