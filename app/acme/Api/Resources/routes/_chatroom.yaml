chatroom:
    type: resource
    resource: chatroom
    path: chatrooms
    methods: [GET]
    only: [ index, show ]
    middleware: ["auth:user"]
    options:
        paginate: true
        withs:
            show:
                - messages.user